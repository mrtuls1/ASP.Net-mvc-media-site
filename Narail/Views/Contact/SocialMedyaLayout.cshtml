@model Narail.Models.Settings
@{
    Layout = null;
}
@if (Model.FacebookLink != null && Model.FacebookLink != "")
{
    <li><a href="@Model.FacebookLink"><i class="fa fa-facebook"></i></a></li>
}
@if (Model.YoutubeLink != null && Model.YoutubeLink!= "")
{
    <li><a href="@Model.YoutubeLink"><i class="fa fa-youtube"></i></a></li>
}
@if (Model.InstagramLink!= null && Model.InstagramLink != "")
{
    <li><a href="@Model.InstagramLink"><i class="fa fa-instagram"></i></a></li>
}
@if (Model.TwitterLink != null && Model.TwitterLink!= "")
{
    <li><a href="@Model.TwitterLink"><i class="fa fa-twitter"></i></a></li>
}
@if (Model.Linledin!= null && Model.Linledin != "")
{
    <li><a href="@Model.Linledin"><i class="fa fa-linkedin"></i></a></li>
}
